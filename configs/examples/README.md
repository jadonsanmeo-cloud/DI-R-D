# Example Configurations

Place sanitized, shareable provider and runtime configuration examples here.
Environment-specific settings belong under `configs/development/` and secrets
must be supplied through environment variables or an ignored `.env` file.
